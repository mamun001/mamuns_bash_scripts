

#ssh ln-nasapp-d01.int.thomsonreuters.com "vfiler run * cifs sessions" 2>/dev/null  > /tmp/cifs.data.1


# from real DFM  the following command should do.
#FILER=ln-nasapp-d01.int.thomsonreuters.com
#ssh #FILER "vfiler run * cifs sessions" 2>/dev/null  > /tmp/cifs.data.1
#grep \( /tmp/cifs.data.1 | grep \)  | cut -d")" -f1 | grep -v PC | sort | uniq | sort > /tmp/cifs.data.2

# this only works on my cygwin
grep \( hold | grep \)  | cut -d")" -f1 | grep -v PC | sort | uniq | sort > /tmp/cifs.data.2



# this part should work on both
echo
echo Number of CIFS hosts
echo
wc -l < /tmp/cifs.data.2
echo
echo lists of hosts
echo
cat /tmp/cifs.data.2
